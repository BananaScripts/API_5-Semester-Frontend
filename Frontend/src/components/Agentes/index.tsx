import React from "react";
import { View, Text } from "react-native";
import { styles } from "./style";

const Agentes = () => {
  return (
    <View style={styles.container}>
      <Text style={styles.title}>Chatbots disponíveis</Text>
    </View>
  );
};

export default Agentes;